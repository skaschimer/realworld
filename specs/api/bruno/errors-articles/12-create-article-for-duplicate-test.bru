meta {
  name: Create article for duplicate test
  type: http
  seq: 12
}

post {
  url: {{host}}/api/articles
  body: json
  auth: none
}

headers {
  Authorization: Token {{token}}
}

body:json {
  {
    "article": {
      "title": "Dup Article {{uid}}",
      "description": "test",
      "body": "test"
    }
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  bru.setVar("slug", res.body.article.slug);
}
